#DATASOURCE
spring.datasource.url= jdbc:mysql://localhost:3307/projetoExemplo?createDatabaseIfNotExist=true
spring.datasource.username= root
spring.datasource.password= root

#JPA
spring.jpa.hibernate.ddl-auto= update

# show-sql setada como true vai imprimir (LOG) das instru��es SQL geradas pelo Hibernate
# que s�o enviadas para o banco de dados
spring.jpa.show-sql= true

# open-in-view habilita o filtro do Spring respons�vel por manter aberta uma
# transa��o at� que o request esteja encerrado
spring.jpa.open-in-view= true

# open-in-view habilita o filtro do Spring respons�vel por manter aberta uma
# transa��o at� que o request esteja encerrado
spring.jpa.hibernate.use-new-id-generator-mappings= false

# THYMELEAF
# desativa o armazenamento em cache do modelo (template)
spring.thymeleaf.cache= false

